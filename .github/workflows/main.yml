name: CI

on: [push]

jobs:
  build-linux:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v1
    - name: Prepare environment
      run: |
           sudo apt install haxe
    - name: Build
      run: |
        make

